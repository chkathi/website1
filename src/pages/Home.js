import React from "react";
import "../App.css";
import { Title } from "./Title";

export const Home = (props) => {
  return (
    <div className="home-page">
      <section>
        <Title />
      </section>
    </div>
  );
};
